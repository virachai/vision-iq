{
	"name": "admin",
	"version": "1.0.0",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "vite build",
		"dev": "sh -c 'set -a; . ../../.env; set +a; vite --host 0.0.0.0 --port $ADMIN_PORT --clearScreen false'",
		"check-types": "tsc --noEmit",
		"lint": "biome check .",
		"start": "sh -c 'set -a; . ../../.env; set +a; vite preview --host 0.0.0.0 --port $ADMIN_PORT --clearScreen false'"
	},
	"dependencies": {
		"@repo/ui": "workspace:*",
		"react": "^19.0.0",
		"react-dom": "^19.0.0"
	},
	"devDependencies": {
		"@repo/biome-config": "workspace:*",
		"@vitejs/plugin-react": "^4.3.4",
		"typescript": "^5.7.3",
		"vite": "^6.0.7"
	}
}
